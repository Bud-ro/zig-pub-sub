{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "preLaunchTask": "zig-build",
            "request": "launch",
            "type": "cppvsdbg",
            "program": "${workspaceFolder}/zig-out/bin/zig-pub-sub.exe",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ],
}